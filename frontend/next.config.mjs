/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com','lh3.googleusercontent.com','127.0.0.1','localhost'], // Add the hostname here
      },
};

export default nextConfig;
